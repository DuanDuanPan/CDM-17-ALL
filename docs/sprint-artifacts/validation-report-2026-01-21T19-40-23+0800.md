# Validation Report

**Document:** docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md  
**Checklist:** _bmad/bmm/workflows/4-implementation/create-story/checklist.md  
**Date:** 2026-01-21T19-40-23+0800  
**Agent Model:** GPT-5.2 (Codex CLI)

## Summary
- Overall: 47/80 passed (58.8%); Partial: 29; Fail: 0; N/A: 4
- Critical Issues: 0

## Section Results

### Critical Mistakes to Prevent
Pass Rate: 4/8 (50.0%)

✓ PASS — Reinventing wheels  
Evidence: Story explicitly migrates existing upload/download/delete implementations to the already-built `FileStorageService` instead of introducing new storage paths (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:27-49,82-91`; `apps/api/src/modules/file-storage/file-storage.service.ts:71-207`).

✓ PASS — Wrong libraries  
Evidence: Stays within existing NestJS + Multer + `nanoid` + Prisma/Repository patterns already used in Story 10.4 (`apps/api/package.json:24-35`; `docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:82-170`).

⚠ PARTIAL — Wrong file locations  
Evidence: Backend targets exist and match repo structure (`apps/api/src/modules/file/file.service.ts`, `apps/api/src/modules/data-management/data-asset.service.ts`, `packages/plugins/plugin-comments/src/server/comments/attachments.controller.ts`) (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:76-80,200-205`).  
Gap: Frontend hook paths are incorrect: `apps/web/hooks/useDataAsset.ts` and `apps/web/hooks/use-comments.ts` are referenced but do not exist (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:174-178`). The likely actual touchpoints are `apps/web/features/data-library/hooks/useDataUpload.ts`, `apps/web/features/data-library/hooks/useDataAssets.ts`, `apps/web/hooks/useAttachmentUpload.ts`, and `apps/web/hooks/useComments.ts` (`apps/web/features/data-library/hooks/useDataUpload.ts:1-33`; `apps/web/features/data-library/hooks/useDataAssets.ts:1-46`; `apps/web/hooks/useAttachmentUpload.ts:99-101`; `apps/web/hooks/useComments.ts:24-45`).  
Impact: Dev agent may update non-existent files and miss the real upload callers, causing runtime regressions.

⚠ PARTIAL — Breaking regressions  
Evidence: Includes automated test commands + manual verification checklist + risk table (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:55-61,230-262`).  
Gaps / risks not explicitly addressed:
- **Nest module wiring risk:** `FileStorageModule` currently imports `FileModule` for legacy fallback (`apps/api/src/modules/file-storage/file-storage.module.ts:18-28`). Task 1.6 proposes making `FileModule` import `FileStorageModule` (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:33`), which can create a circular dependency unless explicitly handled (e.g., remove fallback or use `forwardRef`).  
- **DataAsset `storagePath` semantic change:** Story wants `DataAsset.storagePath = FileRecord.id` (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:37,248`), but current backend + tests expect `/api/files/:id` URLs (`apps/api/src/modules/data-management/data-asset.service.ts:88-107`; `apps/api/src/modules/data-management/__tests__/data-asset.service.spec.ts:242-297`) and web UI/tests also assume URL-like `storagePath` (`apps/web/features/data-library/hooks/useAssetPreview.ts:31-37`; `apps/web/features/data-library/__tests__/AssetCard.test.tsx:11-28`).  
Impact: Without explicit handling, changes will break unit tests and user flows (asset preview/download) even if lint passes.

✓ PASS — Ignoring UX  
Evidence: Explicitly calls out and tracks frontend caller updates + provides manual verification for the three user-facing flows (deliverables/data assets/comment attachments) (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:50-54,172-178,245-251`).

⚠ PARTIAL — Vague implementations  
Evidence: Provides concrete tasks and code-shape examples (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:27-60,95-170`).  
Gaps:
- FileStorageService API table is inaccurate vs current implementation (see below).  
- DataAsset “BEFORE” example uses `uploads/...` path format that does not match current code (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:123-145`; `apps/api/src/modules/data-management/data-asset.service.ts:97-107`).  
- Attachments migration does not explicitly describe the required order if `ownerId=attachmentId` is required (create attachment record first → upload file with `ownerId` → update attachment `storagePath`) (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:41-49,158-169`).  
Impact: Developer may implement incorrect storagePath rules or miss `ownerId`, leading to inconsistent ownership metadata and broken deletes.

✓ PASS — Lying about completion  
Evidence: Story is `ready-for-dev` and all tasks are unchecked; Dev Agent Record is still a template placeholder (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:3,27-60,265-275`).

⚠ PARTIAL — Not learning from past work  
Evidence: Carries forward key Story 10.4 learnings (graphId enforcement + rollback + prod header requirement) (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:211-222`; `apps/api/src/modules/file-storage/guards/file-storage-auth.guard.ts:11-38`).  
Gap: Git reference `ae9e904` cannot be resolved in this repo and Story 10.4’s actual file-storage implementation commit appears as `2d58eef` in git history (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:225`; `apps/api/src/modules/file-storage` git history).  
Impact: Misleading SHAs reduce traceability and may cause dev to look at the wrong baseline.

---

### Step 1: Load and Understand the Target
Pass Rate: 6/6 (100.0%)

✓ PASS — Load the workflow configuration  
Evidence: Workflow defines `sprint_artifacts`, `epics_file`, `architecture_file`, and validation checklist locations (`_bmad/bmm/workflows/4-implementation/create-story/workflow.yaml:6-26`).

✓ PASS — Load the story file  
Evidence: Story file exists with title + `ready-for-dev` status (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:1-4`).

✓ PASS — Load validation framework  
Evidence: Validation framework specifies PASS/PARTIAL/FAIL/N/A marking and report format (`_bmad/core/tasks/validate-workflow.xml:18-71`).

✓ PASS — Extract metadata (epic/story key/title)  
Evidence: Title indicates Story 10.5 and filename matches story key (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:1`).

✓ PASS — Resolve workflow variables  
Evidence: `output_folder` is `docs` and sprint artifacts are `docs/sprint-artifacts` (`_bmad/bmm/config.yaml:8-13`; `_bmad/bmm/workflows/4-implementation/create-story/workflow.yaml:21-26`).

✓ PASS — Understand current status  
Evidence: Story is `ready-for-dev` and sprint tracker matches (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:3`; `docs/sprint-artifacts/sprint-status.yaml:131-137`).

---

### Step 2.1: Epics and Stories Analysis
Pass Rate: 6/6 (100.0%)

✓ PASS — Load epics file  
Evidence: Epic 10 and Story 10.5 are present (`docs/epics.md:1278-1289,1346-1359`).

✓ PASS — Epic objectives and business value  
Evidence: Epic 10 targets repository convergence + unified file storage + plugin migrations to reduce duplication/coupling (`docs/epics.md:1278-1289`).

✓ PASS — ALL stories in this epic (cross-story context)  
Evidence: Stories 10.1–10.9 listed with sequencing; 10.5 sits after 10.4 foundation (`docs/epics.md:1290-1360`; `docs/sprint-artifacts/sprint-status.yaml:131-141`).

✓ PASS — Story requirements and acceptance criteria alignment  
Evidence: Epic Story 10.5 AC matches story file AC wording and scope (`docs/epics.md:1346-1359`; `docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:11-23`).

✓ PASS — Technical requirements and constraints  
Evidence: Epic 10 allows no backward compatibility and ignores historical data; story adopts this (AC#2 and risk mitigation) (`docs/epics.md:1283-1285`; `docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:17-20,261-262`).

✓ PASS — Cross-story dependencies and prerequisites  
Evidence: Story explicitly depends on 10.4 FileStorage foundation and assumes it is done (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:82-91,211-217`; `docs/sprint-artifacts/sprint-status.yaml:135-136`).

---

### Step 2.2: Architecture Deep-Dive
Pass Rate: 4/10 (40.0%)

✓ PASS — Load architecture file  
Evidence: Architecture defines NestJS mandates and testing strategy used for implementation decisions (`docs/architecture.md:685-713`).

✓ PASS — Technical stack with versions  
Evidence: NestJS v11 + Multer + nanoid are pinned in `@cdm/api` dependencies; story uses these (`apps/api/package.json:24-35`; `docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:95-170`).

✓ PASS — Code structure and organization patterns  
Evidence: Story targets module-per-feature paths and references Repository Pattern rules (`docs/project-context.md:95-108`; `docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:74-80,200-205`).

⚠ PARTIAL — API design patterns and contracts  
Evidence: Story correctly identifies `/api/files/*` as the unified entry point and adds `graphId` for comment attachment upload (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:82-91,158-169,192-195`; `apps/api/src/modules/file-storage/file-storage.controller.ts:95-120`).  
Gap: Does not spell out final API contract for DataAsset download/preview (if `storagePath` becomes an id) nor the precise response-shape changes expected by web UI/tests.  
Impact: Client code may break on URL assumptions without a clear migration plan.

⚠ PARTIAL — Database schemas and relationships  
Evidence: Story uses existing `FileRecord` table and owner fields introduced in 10.4 (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:82-91,213-216`).  
Gap: Changes semantics of `DataAsset.storagePath` and `CommentAttachment.storagePath` without specifying whether any backfill or cleanup is required (beyond “ignore historical data”).  
Impact: Old records may become unreadable unless explicitly accepted/cleared, and unit tests expecting URL strings will fail.

⚠ PARTIAL — Security requirements and patterns  
Evidence: Notes prod header requirement for upload/delete (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:221`; `apps/api/src/modules/file-storage/guards/file-storage-auth.guard.ts:11-38`).  
Gap: Does not define a unified MIME validation policy (comment attachments currently enforce `ALLOWED_MIME_TYPES`) and does not clarify whether comment attachments should preserve stricter auth semantics than `FileStorageAuthGuard` in non-prod (`packages/plugins/plugin-comments/src/server/comments/attachments.controller.ts:35-86`; `apps/api/src/modules/file-storage/guards/file-storage-auth.guard.ts:20-38`).  
Impact: Migration could unintentionally broaden allowed uploads or weaken access expectations.

⚠ PARTIAL — Performance requirements and optimization strategies  
Evidence: Existing endpoints use memory upload limits (10MB) (`apps/api/src/modules/file-storage/file-storage.controller.ts:31-33,101-108`; `packages/plugins/plugin-comments/src/server/comments/attachments.controller.ts:34-35,101-106`).  
Gap: Story does not explicitly instruct to retain size limits and streaming behavior implications when moving attachments from disk-streaming to buffer-based reads (`apps/api/src/modules/file-storage/file-storage.service.ts:146-155`).  
Impact: Risk of increased memory usage if limits are loosened or misapplied.

✓ PASS — Testing standards and frameworks  
Evidence: Requires `@cdm/api` Jest + web tests; aligns with project testing guidance (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:182-191,234-241`; `docs/project-context.md:126-129`).

➖ N/A — Deployment and environment patterns  
Evidence: No CI/CD or deployment configuration changes are required for this migration story.

⚠ PARTIAL — Integration patterns and external services  
Evidence: Story expects plugin-comments to use `FileStorageService` (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:41-49`).  
Gap: Does not specify required Nest module wiring: `CommentsServerModule.register()` currently imports no external modules (`apps/api/src/app.module.ts:48-52`; `packages/plugins/plugin-comments/src/server/index.ts:68-83`; `packages/plugins/plugin-comments/src/server/comments/comments.module.ts:34-47`).  
Impact: Injecting `FileStorageService` into `AttachmentsController` will fail at runtime unless `FileStorageModule` is imported into the plugin module graph.

---

### Step 2.3: Previous Story Intelligence
Pass Rate: 6/7 (85.7%)

✓ PASS — Load previous story file  
Evidence: Story 10.4 file exists and is referenced (`docs/sprint-artifacts/10-4-unified-file-storage-foundation.md:1-4`; `docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:201,211-217`).

✓ PASS — Dev notes and learnings  
Evidence: Captures key 10.4 operational learnings: rollback, graphId FK validation, and prod header requirement (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:218-222`; `apps/api/src/modules/file-storage/file-storage.service.ts:82-127`).

⚠ PARTIAL — Review feedback and corrections needed  
Evidence: 10.4 explicitly documents “legacy compatibility” and module wiring decisions (FileStorageController uses legacy fallback; FileStorageModule imports FileModule) (`apps/api/src/modules/file-storage/file-storage.controller.ts:41-93`; `apps/api/src/modules/file-storage/file-storage.module.ts:18-28`; `docs/sprint-artifacts/10-4-unified-file-storage-foundation.md:314-321`).  
Gap: Story 10.5 does not call out how to handle this when migrating `FileService` (Task 1) and potentially introducing a module cycle (Task 1.6).  
Impact: Dev may hit DI/circular-dependency issues or remove legacy support unintentionally.

✓ PASS — Files created/modified and their patterns  
Evidence: Story identifies exact backend + plugin file targets and references 10.4 infrastructure as the baseline (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:74-80,200-205`).

✓ PASS — Testing approaches that worked/didn’t  
Evidence: Reuses established `pnpm --filter @cdm/api test` + `pnpm --filter @cdm/web test` flow (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:234-241`; `docs/project-context.md:126-129`).

✓ PASS — Problems encountered and solutions found  
Evidence: Highlights typical failure modes (missing graphId, dual-writing, outdated frontend calls) and provides explicit verification checks (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:68-72,245-251`).

✓ PASS — Code patterns and conventions established  
Evidence: Anchors changes to repository pattern rules and existing FileStorage abstraction rather than direct prisma usage (`docs/project-context.md:105-108`; `apps/api/src/modules/file-storage/file-storage.repository.ts`).

---

### Step 2.4: Git History Analysis
Pass Rate: 3/5 (60.0%)

⚠ PARTIAL — Files created/modified in previous work  
Evidence: Story references Story 10.4 as the baseline and includes a git SHA (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:201,223-226`).  
Gap: SHA `ae9e904` does not resolve in this repo; the current git history for file-storage shows a different Story 10.4 commit (`apps/api/src/modules/file-storage` git history).  
Impact: Weak traceability increases chance of implementing against the wrong baseline.

✓ PASS — Code patterns and conventions used  
Evidence: File storage code follows NestJS module-per-feature + service/repository separation; story explicitly ties to that and to Repository rules (`docs/project-context.md:97-108`; `apps/api/src/modules/file-storage/file-storage.module.ts:18-40`; `docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:200-205`).

➖ N/A — Library dependencies added/changed  
Evidence: Migration is refactoring/wiring; no new dependencies are required beyond existing NestJS/Multer/nanoid stack (`apps/api/package.json:15-39`).

✓ PASS — Architecture decisions implemented  
Evidence: Epic 10 mandates graphId layering + no backward-compat requirement; story adopts this and focuses on consolidating call sites (`docs/epics.md:1283-1288`; `docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:17-20,55-60`).

✓ PASS — Testing approaches used  
Evidence: Specifies API lint/test and web test runs (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:182-191,234-241`).

---

### Step 2.5: Latest Technical Research
Pass Rate: 1/4 (25.0%)

✓ PASS — Identify libraries/frameworks mentioned  
Evidence: Story operates within NestJS + Multer + existing unified storage module (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:82-170`; `apps/api/package.json:24-35`).

⚠ PARTIAL — Breaking changes or security updates  
Evidence: No external “latest guidance” references are included for file upload security (MIME validation vs content sniffing, path traversal, attachment download auth).  
Impact: Increases risk of subtle security regressions when removing duplicated validators and moving storage code.

➖ N/A — Performance improvements or deprecations  
Evidence: Story is a consolidation/migration; no explicit framework upgrade or deprecation work is in scope.

⚠ PARTIAL — Best practices for current versions  
Evidence: Story references graphId enforcement and rollback patterns from 10.4 (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:218-222`).  
Gap: Does not explicitly restate key best practices for this migration (keep file size limits, preserve attachment auth semantics, ensure plugin DI imports).  
Impact: Dev may implement functionally correct migration but miss important guardrails.

---

### Step 3.1: Reinvention Prevention Gaps
Pass Rate: 3/3 (100.0%)

✓ PASS — Wheel reinvention  
Evidence: Migration explicitly consolidates the three independent implementations into `FileStorageService` (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:13-16,27-49`).

✓ PASS — Code reuse opportunities not identified  
Evidence: Story points to existing FileStorage APIs and shows how to delegate rather than re-implement decoding/path logic (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:82-117`; `apps/api/src/modules/file-storage/file-storage.service.ts:43-69`).

✓ PASS — Existing solutions not mentioned  
Evidence: References Story 10.4 foundation and repository pattern docs (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:201-205`).

---

### Step 3.2: Technical Specification DISASTERS
Pass Rate: 1/5 (20.0%)

✓ PASS — Wrong libraries/frameworks  
Evidence: Reuses existing NestJS modules/services; no new storage libs introduced (`apps/api/package.json:24-35`; `docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:27-49`).

⚠ PARTIAL — API contract violations  
Evidence: Allows API route changes and introduces `graphId` for comment attachment upload (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:17-20,43-44,162-169`).  
Gap: FileStorageService API table is inaccurate (claims `{buffer, metadata}` and returns `FileRecord`) (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:86-91`; `apps/api/src/modules/file-storage/file-storage.service.ts:77-81,146-155,169-185`).  
Impact: Dev may code to the wrong return shape/types and introduce TS/runtime mismatches.

⚠ PARTIAL — Database schema conflicts  
Evidence: Story intends to store `FileRecord.id` in existing `storagePath` fields (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:37,47-48`).  
Gap: Current code/tests assume URL strings and parse `storagePath` as a URL to extract file id (`apps/api/src/modules/data-management/data-asset.service.ts:97-107,410-425`; `apps/api/src/modules/data-management/__tests__/data-asset.service.spec.ts:242-297`).  
Impact: Without careful migration or response mapping, deletes/previews break and tests fail.

⚠ PARTIAL — Security vulnerabilities  
Evidence: Current comment attachments enforce `ALLOWED_MIME_TYPES` and strict `x-user-id` checks (`packages/plugins/plugin-comments/src/server/comments/attachments.controller.ts:35-47,108-118`).  
Gap: Story does not define whether to preserve MIME allowlist and auth behavior when delegating to FileStorageService (which currently does not enforce allowlists).  
Impact: Potentially broadens upload surface or changes download authorization expectations.

⚠ PARTIAL — Performance disasters  
Evidence: Current comment attachment download streams from disk (`packages/plugins/plugin-comments/src/server/comments/attachments.controller.ts:192-195`), while FileStorageService downloads via full-buffer read (`apps/api/src/modules/file-storage/file-storage.service.ts:146-155`).  
Gap: Story does not state whether streaming is required or acceptable to change.  
Impact: Large file handling could regress if future size limits increase; needs explicit guardrails.

---

### Step 3.3: File Structure DISASTERS
Pass Rate: 1/4 (25.0%)

⚠ PARTIAL — Wrong file locations  
Evidence: Core backend file targets exist (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:76-80,200-205`).  
Gap: Frontend hook paths are wrong and may cause dev to miss real callers (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:174-178`; `apps/web/hooks/useAttachmentUpload.ts:99-101`; `apps/web/features/data-library/hooks/useDataUpload.ts:1-33`).

✓ PASS — Coding standard violations  
Evidence: Story reinforces repository pattern and encourages using existing services/modules rather than direct prisma in services (`docs/project-context.md:105-108`; `docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:68-72`).

⚠ PARTIAL — Integration pattern breaks  
Evidence: Story changes cross-module boundaries (core module + data-management module + plugin-comments) (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:27-54`).  
Gap: Missing explicit Nest DI/module-import steps for plugin-comments to access `FileStorageService` (`apps/api/src/app.module.ts:48-52`; `packages/plugins/plugin-comments/src/server/comments/comments.module.ts:34-47`).  
Impact: Runtime DI errors are likely if module imports aren’t updated.

➖ N/A — Deployment failures  
Evidence: No deployment config modifications are involved.

---

### Step 3.4: Regression DISASTERS
Pass Rate: 0/4 (0.0%)

⚠ PARTIAL — Breaking changes  
Evidence: Story acknowledges API routes may change and includes rollback considerations (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:17-20,255-262`).  
Gap: Does not define how to avoid breaking existing DataAsset previews/downloads when `storagePath` semantics change, nor how to handle legacy `/api/files/:id` behavior if FileService is refactored (`apps/api/src/modules/file-storage/file-storage.controller.ts:41-93`; `docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:27-33,37`).  
Impact: Users may lose access to uploaded assets/attachments during migration.

⚠ PARTIAL — Test failures  
Evidence: Requires running API/web tests (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:234-241`).  
Gap: Does not call out specific tests that will need updates (e.g., DataAssetService spec expects `/api/files/...` storagePath) (`apps/api/src/modules/data-management/__tests__/data-asset.service.spec.ts:242-297`; `apps/web/features/data-library/__tests__/AssetCard.test.tsx:11-28`).  
Impact: Dev may underestimate scope/time; CI may fail until tests are updated.

⚠ PARTIAL — UX violations  
Evidence: Story intends to keep UX flows working via frontend adaptation tasks (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:50-54,245-251`).  
Gap: Frontend caller identification is inaccurate (attachments upload is in `useAttachmentUpload`, not `useComments`), risking broken upload UX (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:53,174-178`; `apps/web/hooks/useAttachmentUpload.ts:99-101`).  
Impact: Comment attachments upload may silently break after API changes.

⚠ PARTIAL — Learning failures  
Evidence: References 10.4 and key lessons (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:211-222`).  
Gap: Does not incorporate 10.4’s “legacy fallback imports FileModule” design when instructing FileModule→FileStorageModule import (`apps/api/src/modules/file-storage/file-storage.module.ts:18-28`; `docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:33`).  
Impact: High chance of repeating integration mistakes (DI cycles).

---

### Step 3.5: Implementation DISASTERS
Pass Rate: 2/4 (50.0%)

⚠ PARTIAL — Vague implementations  
Evidence: Tasks are enumerated, but key technical details are inaccurate/outdated (FileStorageService API table, DataAsset BEFORE example) (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:86-91,123-145`; `apps/api/src/modules/file-storage/file-storage.service.ts:77-185`; `apps/api/src/modules/data-management/data-asset.service.ts:88-107`).  
Impact: Higher risk of wrong implementations or extra rework.

✓ PASS — Completion lies  
Evidence: Acceptance criteria are explicit and verifiable, and verification plan includes automated tests (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:11-23,230-241`).

✓ PASS — Scope creep  
Evidence: Scope is constrained to three caller areas + minimal frontend wiring + cleanup/testing; explicitly allows API route changes without compatibility burden (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:13-23,25-61`).

⚠ PARTIAL — Quality failures  
Evidence: Includes lint/test requirements (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:234-241`).  
Gap: No explicit checklist for updating impacted unit tests and web components that use `storagePath` as a URL.  
Impact: “Tests pass” goal may be delayed by surprise regressions.

---

### Step 4: LLM-Dev-Agent Optimization Analysis
Pass Rate: 3/5 (60.0%)

✓ PASS — Verbosity problems  
Evidence: Story is reasonably concise and uses tables/snippets to compress critical context (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:74-80,82-170`).

⚠ PARTIAL — Ambiguity issues  
Evidence: Some core fields/paths are ambiguous or wrong (FileStorageService return types, web hook paths, storagePath semantics) (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:86-91,37,174-178`).  
Impact: LLM dev agent may take incorrect actions or miss required edits.

✓ PASS — Context overload  
Evidence: Context focuses on the three migration targets plus tests; avoids unrelated epic scope (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:25-61,74-80`).

⚠ PARTIAL — Missing critical signals  
Evidence: Highlights “graphId required” and “avoid dual write” (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:68-72`).  
Gap: Does not highlight the most failure-prone wiring details (plugin module imports; FileStorageModule↔FileModule cycle).  
Impact: High risk of runtime DI breakage.

✓ PASS — Poor structure  
Evidence: Clear flow: AC → Tasks → Dev Notes → References → Previous Story Intelligence → Verification → Risks (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:11-275`).

---

### Step 4: LLM Optimization Principles
Pass Rate: 3/5 (60.0%)

✓ PASS — Clarity over verbosity  
Evidence: Tasks are broken down by module and map to acceptance criteria (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:27-61`).

⚠ PARTIAL — Actionable instructions  
Evidence: Provides actionable steps but with some incorrect file targets and outdated examples (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:123-145,174-178`).  
Impact: Developer may waste time reconciling inconsistencies.

✓ PASS — Scannable structure  
Evidence: Uses headings, checklists, tables, and code blocks for quick scanning (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:25-61,74-80,245-252`).

✓ PASS — Token efficiency  
Evidence: Focuses on the minimal set of callers and defers deeper feature work (e.g., fine-grained permissions) consistent with Epic 10 constraints (`docs/epics.md:1283-1285`; `docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:17-20`).

⚠ PARTIAL — Unambiguous language  
Evidence: Story says `storagePath` should store `FileRecord.id` but does not define how API responses and UI should treat it (URL vs id), and references incorrect hook locations (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:37,245-249,174-178`).  
Impact: Multiple valid interpretations → inconsistent implementation.

---

### Step 5: Improvement Recommendations
Pass Rate: 4/4 (100.0%)

✓ PASS — Critical Misses (Must Fix)  
Evidence: Addressed in “Recommendations” section below (per checklist `_bmad/bmm/workflows/4-implementation/create-story/checklist.md:Category 1`).

✓ PASS — Enhancement Opportunities (Should Add)  
Evidence: Addressed in “Recommendations” section below.

✓ PASS — Optimization Suggestions (Nice to Have)  
Evidence: Addressed in “Recommendations” section below.

✓ PASS — LLM Optimization Improvements  
Evidence: Addressed in “Recommendations” section below.

---

## Failed Items
- None.

## Partial Items
- Frontend file path mismatches for data assets + comment attachments callers (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:174-178`).
- Potential Nest module circular dependency if `FileModule` imports `FileStorageModule` while `FileStorageModule` already imports `FileModule` (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:33`; `apps/api/src/modules/file-storage/file-storage.module.ts:18-28`).
- FileStorageService API table return types mismatch (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:86-91`; `apps/api/src/modules/file-storage/file-storage.service.ts:77-185`).
- DataAsset `storagePath` semantics change conflicts with current backend parsing logic + tests + web UI expectations (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:37,248`; `apps/api/src/modules/data-management/data-asset.service.ts:97-107,410-425`; `apps/api/src/modules/data-management/__tests__/data-asset.service.spec.ts:242-297`; `apps/web/features/data-library/hooks/useAssetPreview.ts:31-37`).
- Plugin-comments DI wiring not specified for injecting `FileStorageService` into `AttachmentsController` (`apps/api/src/app.module.ts:48-52`; `packages/plugins/plugin-comments/src/server/index.ts:68-83`; `packages/plugins/plugin-comments/src/server/comments/comments.module.ts:34-47`).
- Attachments `ownerId=attachmentId` flow not explicitly sequenced (create record first vs upload first) (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:41-49,158-169`).
- Git SHA reference `ae9e904` does not resolve in current repo history (`docs/sprint-artifacts/10-5-migrate-callers-to-file-storage.md:225`).

## Recommendations
1. Must Fix: Clarify module wiring to avoid a `FileStorageModule`↔`FileModule` cycle (either remove legacy fallback from FileStorageModule/Controller, or explicitly use `forwardRef` and document the decision).  
2. Must Fix: Correct the FileStorageService API table to match current signatures/return shapes (`upload → FileMetadataDto`, `download → {buffer, record}`) and update code examples accordingly (`apps/api/src/modules/file-storage/file-storage.service.ts:77-185`).  
3. Must Fix: Fix frontend caller references: data assets upload uses `apps/web/features/data-library/hooks/useDataUpload.ts`, comment attachment upload uses `apps/web/hooks/useAttachmentUpload.ts`, comments fetching is `apps/web/hooks/useComments.ts` (`apps/web/hooks/useAttachmentUpload.ts:99-101`).  
4. Must Fix: Define the `DataAsset.storagePath` contract post-migration (DB stores fileId vs API returns URL), and list the concrete backend/web/test updates required so CI is predictable (e.g., update `data-asset.service.spec.ts` and affected web components/tests).  
5. Should Improve: Add explicit plugin-comments DI instructions (e.g., import `FileStorageModule` into `CommentsServerModule.forRoot({ imports: [...] })` in `apps/api/src/app.module.ts`) and call out any auth/MIME policies to preserve from the current attachments controller.  
6. Consider: Add a short “legacy data policy” note: whether old `uploads/comments` and `/api/files/:id` legacy downloads must keep working during the sprint, or whether they’re intentionally broken under Epic 10’s “ignore history” constraint (`docs/epics.md:1283-1285`).
