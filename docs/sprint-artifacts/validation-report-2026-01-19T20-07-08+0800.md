# Validation Report

**Document:** docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md  
**Checklist:** _bmad/bmm/workflows/4-implementation/create-story/checklist.md  
**Date:** 2026-01-19T20-07-08+0800  
**Agent Model:** GPT-5.2 (Codex CLI)

## Summary
- Overall: 28/80 passed (35.0%); Partial: 35; Fail: 4; N/A: 13
- Critical Issues: 4

## Section Results

### Critical Mistakes to Prevent
Pass Rate: 3/8 (37.5%)

⚠ PARTIAL — Reinventing wheels  
Evidence: Story proposes new shared utilities (`apps/web/lib/parentIdUtils.ts`, `apps/web/lib/edgeReconciler.ts`) (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:81-117`), but parentId-as-truth patterns already exist (templates store only dependency edges; hierarchy derived from children/parentId) (`packages/types/src/template-types.ts:35-47`).  
Impact: Dev may duplicate hierarchy helpers instead of consolidating existing patterns into the new utilities.

✓ PASS — Wrong libraries  
Evidence: Story aligns with repo stack: AntV X6 + Yjs are the actual graph/sync stack (`docs/project-context.md:21-27`) and referenced types/utilities exist (`apps/web/lib/edgeValidation.ts:6-58`; `apps/web/lib/edgeShapes.ts:10-12`).  

⚠ PARTIAL — Wrong file locations  
Evidence: Most cited paths/line anchors exist (e.g. `useLayoutPlugin.ts:259`, `GraphSyncManager.ts:650`) (`apps/web/hooks/useLayoutPlugin.ts:242-265`; `apps/web/features/collab/GraphSyncManager.ts:650-705`), but the Tech-Spec link is a local absolute `file:///...` URI (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:580`).  
Impact: Cross-machine review / remote agents won’t be able to open the referenced Tech-Spec reliably.

✗ FAIL — Breaking regressions  
Evidence: Story scope misses critical hierarchy writers: template insertion writes hierarchical edges into Yjs (`apps/web/hooks/useTemplateInsert.ts:97-123`) and clipboard paste ensures/creates hierarchical edges in Yjs (`apps/web/hooks/clipboard/clipboardPaste.ts:190-292`; `apps/web/hooks/clipboard/pasteHelpers.ts:40-73,113-147`). These are not listed in “影响范围/Tasks” (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:24-35,79-146`).  
Impact: Even if “read children by parentId” is implemented, hierarchy edges will continue to be created/synced, reintroducing dual-truth and causing hard-to-debug cross-client inconsistencies.

✓ PASS — Ignoring UX  
Evidence: AC5 + dedicated E2E cases explicitly target the RCA reproduction (“collapse → click visible node should not reparent”) (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:66-72,444-472`).  

⚠ PARTIAL — Vague implementations  
Evidence: AC3 says “仅同步 parentId 和 order 到 Yjs” (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:52-58`), but current sync writes a full node payload on data changes (`apps/web/features/collab/GraphSyncManager.ts:400-435`). Test plan references helper APIs not listed in tasks (`buildChildrenMap`, `reconcileSingleNodeEdge`) (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:300-307,357-366,550-552`).  
Impact: Dev may over-restrict Yjs node sync (breaking other collaborative fields) and/or miss key helper functions needed for performance and correctness.

✓ PASS — Lying about completion  
Evidence: Story status is `ready-for-dev` with unchecked tasks; no claims of implemented tests or “✅ complete coverage” are present (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:3,79-146`).  

⚠ PARTIAL — Not learning from past work  
Evidence: Story cites key consumers (outline/collapse/drilldown/focus) but misses other established hierarchy code paths (template insert + clipboard Yjs edge creation; outline reorder still directly mutates hierarchical edges) (`apps/web/hooks/useTemplateInsert.ts:97-123`; `apps/web/hooks/clipboard/clipboardPaste.ts:190-292`; `apps/web/components/graph/hooks/useOutlineData.ts:245-269`).  
Impact: Implementation can “pass local ACs” while the system still generates/syncs hierarchical edges, undermining the single-source-of-truth goal.

---

### Step 1: Load and Understand the Target
Pass Rate: 6/6 (100.0%)

✓ PASS — Load the workflow configuration  
Evidence: Workflow config exists and defines inputs/variables (`_bmad/bmm/workflows/4-implementation/create-story/workflow.yaml:1-33`).  

✓ PASS — Load the story file  
Evidence: Story file exists with title + status (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:1-4`).  

✓ PASS — Load validation framework  
Evidence: Validation framework defined (`_bmad/core/tasks/validate-workflow.xml:1-88`).  

✓ PASS — Extract metadata (epic/story key/title)  
Evidence: Story title indicates 8.10 and scope (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:1`).  

✓ PASS — Resolve workflow variables  
Evidence: `output_folder` and `sprint_artifacts` are defined (`_bmad/bmm/config.yaml:8-13`; `_bmad/bmm/workflows/4-implementation/create-story/workflow.yaml:6-13,21-33`).  

✓ PASS — Understand current status  
Evidence: Status is `ready-for-dev` (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:3`).  

---

### Step 2.1: Epics and Stories Analysis
Pass Rate: 2/6 (33.3%)

✓ PASS — Load epics file  
Evidence: Epic 8 is present (`docs/epics.md:771-790`).  

✓ PASS — Epic objectives and business value  
Evidence: Epic 8 targets 500–5000+ node UX and “不迷路、不卡顿、好管理” (`docs/epics.md:771-773`).  

⚠ PARTIAL — ALL stories in this epic (cross-story context)  
Evidence: Sprint status lists related stories (8.1/8.4/8.5/8.9) (`docs/sprint-artifacts/sprint-status.yaml:103-118`) but the story’s “影响范围” table omits major affected hierarchy writers (template insert + clipboard) (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:24-35`).  
Impact: Cross-story dependency tracking can drift; dev may “fix readers” but miss ongoing writers that reintroduce inconsistency.

⚠ PARTIAL — Story requirements and acceptance criteria alignment  
Evidence: Story has clear ACs and tasks, but AC3 wording conflicts with current sync responsibilities (node sync is broader than parentId/order) (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:52-58`; `apps/web/features/collab/GraphSyncManager.ts:400-435`).  
Impact: Dev may implement an over-aggressive sync change and break collaboration for non-structural node fields.

⚠ PARTIAL — Technical requirements and constraints  
Evidence: Story references RCA and key files, but doesn’t anchor key constraints from project context (Yjs-first dataflow and versions) (`docs/project-context.md:14-27,75-82`).  
Impact: Risk of implementing reconcile in a way that causes echo loops or heavy Yjs traffic.

⚠ PARTIAL — Cross-story dependencies and prerequisites  
Evidence: Story touches focus/drilldown/outline/collapse but doesn’t call out that template insertion and clipboard flows currently write hierarchical edges to Yjs (`apps/web/hooks/useTemplateInsert.ts:97-123`; `apps/web/hooks/clipboard/clipboardPaste.ts:190-292`).  
Impact: ParentId may become “truth” for some UI features, while data creation pipelines still produce hierarchical edges, keeping the system inconsistent.

---

### Step 2.2: Architecture Deep-Dive
Pass Rate: 2/10 (20.0%)

⚠ PARTIAL — Technical stack with versions (languages, frameworks, libraries)  
Evidence: Repo versions are defined (`docs/project-context.md:14-43`), but the story does not tie reconciliation/sync strategies to X6/Yjs versions or repo rules (e.g., Yjs-first constraints).  
Impact: Dev may implement patterns that fight the existing collaboration model (extra writes, event loops).

✓ PASS — Code structure and organization patterns  
Evidence: Proposed utility placement under `apps/web/lib/` matches existing shared graph utilities (`apps/web/lib/edgeValidation.ts:1-58`; `apps/web/lib/edgeRouting.ts:1-120`).  

➖ N/A — API design patterns and contracts  
Evidence: No API surface is changed in this story; it is frontend graph model + collaboration behavior only.  

➖ N/A — Database schemas and relationships  
Evidence: No DB schema or migration changes are required for this story.  

➖ N/A — Security requirements and patterns  
Evidence: No auth/ACL boundaries are modified; the change is internal graph structure consistency.  

⚠ PARTIAL — Performance requirements and optimization strategies  
Evidence: Story’s baseline approach for `getDirectChildrenByParentId` scans `graph.getNodes()` every call (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:149-165`). In large graphs, naive usage across multiple callers can become O(n²).  
Impact: Large-graph UX (Epic 8) risks regressions if hierarchy queries aren’t backed by a cached `parentId -> children` map.

✓ PASS — Testing standards and frameworks  
Evidence: Test plan aligns with repo testing stack: Vitest for unit tests and Playwright for E2E (`docs/project-context.md:39-42`; `docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:507-525`).  

➖ N/A — Deployment and environment patterns  
Evidence: No deploy/infra changes are required.  

⚠ PARTIAL — Integration patterns and external services  
Evidence: Project rule is Yjs-first one-way flow (`docs/project-context.md:75-82`). Story proposes to stop syncing hierarchical edges and derive locally, but does not cover all Yjs writers (template insert/clipboard) and does not specify how to prevent reconcile-added hierarchical edges from being echoed back to Yjs without relying solely on GraphSyncManager changes.  
Impact: High risk of “works locally, drifts in collaboration” due to partial enforcement of the new rule.

➖ N/A — External services (Auth, third-party integrations)  
Evidence: No third-party integrations are introduced or modified.  

---

### Step 2.3: Previous Story Intelligence
Pass Rate: 3/7 (42.9%)

✓ PASS — Load previous story file  
Evidence: Predecessor Story 8.9 exists and is marked done (`docs/sprint-artifacts/story-8-9-subgraph-drill-down.md:1-12`; `docs/sprint-artifacts/sprint-status.yaml:112-115`).  

⚠ PARTIAL — Dev notes and learnings  
Evidence: Story references `useDrillDown.ts` lines, but does not summarize the key implementation constraints from 8.9 (drilldown visibility is currently edge-derived and listens to `edge:*` mutations) (`apps/web/components/graph/hooks/useDrillDown.ts:188-200,287-345`).  
Impact: Dev may underestimate the blast radius of switching drilldown from edges to parentId (events, cache, and reapply timing).

➖ N/A — Review feedback and corrections needed  
Evidence: This story is not implemented yet; no review feedback exists to incorporate.  

✓ PASS — Files created/modified and their patterns  
Evidence: Story lists concrete touchpoints across web hooks/components and collab manager (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:202-214`).  

⚠ PARTIAL — Testing approaches that worked/didn’t work  
Evidence: Story proposes new tests but doesn’t reference existing tests that will be impacted (e.g., GraphSyncManager edge sync tests that currently assert hierarchical edges are applied from Yjs) (`apps/web/__tests__/features/GraphSyncManager.test.ts:272-307,350-378`).  
Impact: Dev may miss necessary test updates and misinterpret failures as regressions unrelated to the story.

✓ PASS — Problems encountered and solutions found  
Evidence: RCA document is referenced and the causal chain is explained (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:13-23`; `docs/analysis/node-disappears-on-click-after-collapse-rca-2026-01-19.md:44-118`).  

⚠ PARTIAL — Code patterns and conventions established  
Evidence: Repo already treats hierarchy as “parentId + order” in some domains (templates) (`packages/types/src/template-types.ts:23-47`), but story doesn’t explicitly reuse this as the canonical cross-feature rule (clipboard/template insert currently still create hierarchical edges in Yjs).  
Impact: Inconsistent application of the new “single truth” rule across features.

---

### Step 2.4: Git History Analysis
Pass Rate: 0/5 (0.0%)

⚠ PARTIAL — Files created/modified in previous work  
Evidence: Story does not cite recent commits or blame context for affected modules.  
Impact: Dev may miss subtle constraints introduced by recent bugfixes (e.g., edge routing behavior tied to layout modes).

⚠ PARTIAL — Code patterns and conventions used  
Evidence: No commit pointers or “existing pattern” references (e.g., known-safe ways to update parentId/order) are included.  
Impact: Higher chance of reintroducing previously-fixed issues.

⚠ PARTIAL — Library dependencies added/changed  
Evidence: Story does not note that no deps are required (or confirm existing versions).  
Impact: Minor, but can lead to unnecessary “let’s add a lib” detours.

⚠ PARTIAL — Architecture decisions implemented  
Evidence: Story doesn’t cite history behind the current “edges + parentId” dual system (why it exists, what prior refactors assumed).  
Impact: Refactor may fight existing architectural decisions without explicit tradeoffs.

⚠ PARTIAL — Testing approaches used  
Evidence: Story doesn’t cite existing test patterns for X6/Yjs integration (GraphSyncManager tests, E2E harness patterns).  
Impact: Higher test maintenance cost and duplicated fixtures.

---

### Step 2.5: Latest Technical Research
Pass Rate: 1/4 (25.0%)

✓ PASS — Identify libraries/frameworks mentioned  
Evidence: Story correctly targets X6 + Yjs + layout plugin interaction (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:13-22`; `docs/analysis/node-disappears-on-click-after-collapse-rca-2026-01-19.md:12-18`).  

➖ N/A — Breaking changes or security updates  
Evidence: No dependency upgrades are part of this story.  

➖ N/A — Performance improvements or deprecations  
Evidence: No library version change is required; only feature logic changes.  

➖ N/A — Best practices for current versions  
Evidence: Not applicable without a dependency/version change request.  

---

### Step 3.1: Reinvention Prevention Gaps
Pass Rate: 0/3 (0.0%)

⚠ PARTIAL — Wheel reinvention  
Evidence: Story introduces new hierarchy utilities without explicitly migrating or deleting existing hierarchy writers (outline reorder, clipboard/template edge creation).  
Impact: Duplicate hierarchy implementations can coexist and diverge.

⚠ PARTIAL — Code reuse opportunities not identified  
Evidence: Existing code already builds parentId-based child lists with stable order fallback (`apps/web/lib/subtree-extractor.ts:145-186`) and template types explicitly state hierarchy is derived (`packages/types/src/template-types.ts:35-47`).  
Impact: Dev may recreate helper logic rather than centralizing it into `parentIdUtils.ts`.

⚠ PARTIAL — Existing solutions not mentioned  
Evidence: Outline reorder currently updates both hierarchical edges and `parentId` in a single batch (`apps/web/components/graph/hooks/useOutlineData.ts:245-269`), and GraphSyncManager already filters UI-only keys on node data changes (`apps/web/features/collab/GraphSyncManager.ts:196-223`).  
Impact: Without leveraging these patterns, reconcile may be implemented in a noisy or inconsistent way.

---

### Step 3.2: Technical Specification DISASTERS
Pass Rate: 1/5 (20.0%)

✓ PASS — Wrong libraries/frameworks  
Evidence: All referenced primitives exist and match repo stack (X6 Graph/Node, Yjs, dependency edge metadata) (`docs/project-context.md:21-27`; `apps/web/lib/edgeValidation.ts:29-58`).  

➖ N/A — API contract violations  
Evidence: No API endpoints or DTO contracts are introduced/changed by this story.  

➖ N/A — Database schema conflicts  
Evidence: No DB writes or schema changes are in scope.  

➖ N/A — Security vulnerabilities  
Evidence: No permission/auth model is modified.  

⚠ PARTIAL — Performance disasters prevented  
Evidence: Story requires scanning nodes by `parentId` for children (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:40-43,149-165`) but does not mandate building/caching a children map for large graphs.  
Impact: In 500–5000+ node graphs, repeated “scan all nodes” lookups across multiple features can noticeably degrade UX.

---

### Step 3.3: File Structure DISASTERS
Pass Rate: 1/4 (25.0%)

⚠ PARTIAL — Wrong file locations  
Evidence: Cited code files exist, but story includes local absolute `file:///...` links (Tech-Spec reference) (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:580`).  
Impact: Cross-machine path resolution fails for reviewers/dev agents.

✓ PASS — Coding standard violations  
Evidence: Proposed additions follow repo conventions (TypeScript-first, utilities under `apps/web/lib/`, tests under `apps/web/__tests__` / `apps/web/e2e`) (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:81-146,506-526`; `docs/project-context.md:39-42`).  

✗ FAIL — Integration pattern breaks  
Evidence: Story changes the “source of truth” but does not include all integration touchpoints where hierarchical edges are created/synced (template insert + clipboard) and does not explicitly remove direct hierarchical edge mutation in outline reorder (`apps/web/hooks/useTemplateInsert.ts:97-123`; `apps/web/hooks/clipboard/clipboardPaste.ts:190-292`; `apps/web/components/graph/hooks/useOutlineData.ts:245-269`).  
Impact: The system will continue producing hierarchical edges in Yjs and local graph, keeping dual truth alive and causing inconsistent behavior across features and clients.

➖ N/A — Deployment failures  
Evidence: No deployment/build pipeline changes are required.  

---

### Step 3.4: Regression DISASTERS
Pass Rate: 1/4 (25.0%)

✗ FAIL — Breaking changes  
Evidence: Stopping hierarchical edge sync requires updating all writers and all readers. Story covers several readers but misses key writers (template insert/clipboard) and a reader-writer hybrid (outline reorder) (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:24-35,79-146`; `apps/web/hooks/useTemplateInsert.ts:97-123`; `apps/web/hooks/clipboard/clipboardPaste.ts:190-292`).  
Impact: After partial rollout, some operations will still create hierarchical edges while others ignore them, producing new inconsistent states and regressions in collaboration.

⚠ PARTIAL — Test failures  
Evidence: Story adds new tests but does not explicitly require updating existing GraphSyncManager tests that currently assert hierarchical edges are applied from Yjs (`apps/web/__tests__/features/GraphSyncManager.test.ts:272-307,350-378`).  
Impact: CI will fail or, worse, tests may be deleted/relaxed without equivalent coverage.

✓ PASS — UX violations  
Evidence: Story directly addresses the RCA UX bug with drag threshold + hidden-node filtering + E2E reproduction cases (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:121-146,444-490`; `apps/web/hooks/useLayoutPlugin.ts:242-276,337-343`).  

⚠ PARTIAL — Learning failures  
Evidence: Story references RCA but does not incorporate known “existing hierarchy derived” precedent from templates and does not enumerate all current hierarchy mutation points.  
Impact: Risk of repeating the “dual truth” mistake in new code paths.

---

### Step 3.5: Implementation DISASTERS
Pass Rate: 2/4 (50.0%)

⚠ PARTIAL — Vague implementations  
Evidence: AC3’s “only sync parentId/order” is underspecified relative to current full-node sync behavior (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:52-58`; `apps/web/features/collab/GraphSyncManager.ts:400-435`). Also, test plan references helper APIs not listed as tasks (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:300-307,357-366`).  
Impact: Implementers may make incorrect assumptions, leading to broken collaboration or poor performance.

✓ PASS — Completion lies  
Evidence: No false “completed” indicators are present; tasks remain unchecked (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:79-146`).  

✓ PASS — Scope creep  
Evidence: Story clearly excludes migration and dependency edge changes (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:60-65,332-336`).  

⚠ PARTIAL — Quality failures  
Evidence: Engineering guardrails prohibit direct hierarchical edge manipulation, but tasks do not explicitly require removing existing edge writes (outline reorder, clipboard, template insert) (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:545-571`).  
Impact: Without aligning tasks to guardrails, code may ship that violates the intended architecture.

---

### Step 4: LLM-Dev-Agent Optimization Analysis
Pass Rate: 1/5 (20.0%)

⚠ PARTIAL — Verbosity problems  
Evidence: Story includes a large, repeated “Test Design” section with many placeholder blocks (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:221-525`).  
Impact: Higher risk of token truncation or missing the truly critical invariants.

⚠ PARTIAL — Ambiguity issues  
Evidence: AC3 can be read as “stop syncing all other node fields” (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:52-58`). AC1 uses “getDirectChildren(parentId)” wording that can confuse nodeId vs parentId naming.  
Impact: Different dev agents may implement different interpretations.

⚠ PARTIAL — Context overload  
Evidence: Story mixes architecture, implementation snippets, and long test pseudocode in one file without a concise “single source of truth” summary for the new invariants.  
Impact: Harder for a dev agent to quickly extract “what must be true after every mutation”.

✗ FAIL — Missing critical signals  
Evidence: Story does not list the primary hierarchy edge writers in tasks/scope (template insert + clipboard paste helpers + outline reorder edge mutations) despite them being the highest-risk places to reintroduce dual truth (`apps/web/hooks/useTemplateInsert.ts:97-123`; `apps/web/hooks/clipboard/clipboardPaste.ts:190-292`; `apps/web/components/graph/hooks/useOutlineData.ts:245-269`).  
Impact: Dev agent can implement the story “as written” and still fail the intended architectural outcome.

✓ PASS — Poor structure  
Evidence: Headings/ACs/tasks/dev notes/guardrails are clearly separated and scannable (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:5-214,527-571`).  

---

### Step 4: LLM Optimization Principles
Pass Rate: 1/5 (20.0%)

⚠ PARTIAL — Clarity over verbosity  
Evidence: The most critical “must update” files (template insert + clipboard paste) are absent from the main scope, while many test placeholders are included.  
Impact: Dev agent may spend time on low-signal detail and miss critical integration points.

⚠ PARTIAL — Actionable instructions  
Evidence: Tasks are explicit for the listed files but incomplete for the overall system change (missing required writers/edge mutations).  
Impact: A task-completing dev agent can still ship an incomplete refactor.

✓ PASS — Scannable structure  
Evidence: Clear sections and tables aid navigation.  

⚠ PARTIAL — Token efficiency  
Evidence: Large test pseudocode blocks repeat patterns; could be condensed into a smaller “test matrix + key MREs” section.  
Impact: Increased risk of truncation/missed requirements in LLM execution.

⚠ PARTIAL — Unambiguous language  
Evidence: AC3 wording is ambiguous about which node fields remain synced; “getDirectChildren(parentId)” naming is ambiguous.  
Impact: Implementation divergence across agents.

---

### Step 5: Improvement Recommendations
Pass Rate: 4/4 (100.0%)

✓ PASS — Critical Misses (Must Fix)  
Evidence: Addressed in “Recommendations” section below.  

✓ PASS — Enhancement Opportunities (Should Add)  
Evidence: Addressed in “Recommendations” section below.  

✓ PASS — Optimization Suggestions (Nice to Have)  
Evidence: Addressed in “Recommendations” section below.  

✓ PASS — LLM Optimization Improvements  
Evidence: Addressed in “Recommendations” section below.  

---

## Failed Items
- ✗ Breaking regressions: missing hierarchy writers (template insert + clipboard + outline reorder edge mutations) while attempting to make parentId the only truth (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:24-35,79-146`; `apps/web/hooks/useTemplateInsert.ts:97-123`; `apps/web/hooks/clipboard/clipboardPaste.ts:190-292`; `apps/web/components/graph/hooks/useOutlineData.ts:245-269`).
- ✗ Integration pattern breaks: story doesn’t enforce “no hierarchical edges in Yjs” across all Yjs writers; dual-truth can persist.
- ✗ Breaking changes: partial rollout will create inconsistent states across clients and features.
- ✗ Missing critical signals: the story omits the highest-risk touchpoints from scope/tasks, allowing incomplete implementation.

## Partial Items (Top Risks)
- AC3 ambiguity: clarify “stop syncing hierarchical edges” vs “stop syncing all other node fields” (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:52-58`; `apps/web/features/collab/GraphSyncManager.ts:400-435`).
- Performance: mandate a cached `parentId -> children[]` map (or equivalent) to avoid O(n²) patterns on 500–5000+ node graphs (`docs/epics.md:771-773`; `docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:149-165`).
- Task/test mismatch: tests mention `buildChildrenMap` + `reconcileSingleNodeEdge` but tasks don’t require implementing them (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:300-307,357-366,550-552`).
- Documentation portability: replace `file:///...` links with repo-relative links (`docs/sprint-artifacts/story-8-10-parentid-single-source-of-truth.md:580`).

## Recommendations
1. Must Fix:
   - Add missing scope/tasks for hierarchy writers:
     - `apps/web/hooks/useTemplateInsert.ts` — stop writing hierarchical edges into Yjs; rely on `parentId` + reconcile.
     - `apps/web/hooks/clipboard/clipboardPaste.ts` + `apps/web/hooks/clipboard/pasteHelpers.ts` — stop creating/ensuring hierarchical edges in Yjs; rely on `parentId` + reconcile.
     - `apps/web/components/graph/hooks/useOutlineData.ts` — remove direct hierarchical edge add/remove in reorder; update only `parentId/order` and trigger reconcile.
   - Clarify AC3: keep syncing full node data as today; change is “hierarchical edges are not synced; structure is derived from `parentId/order`”.
   - Add explicit performance requirement: implement `buildChildrenMap(parentId -> sorted children)` and use it across outline/collapse/drilldown/focus + reconcile.
2. Should Improve:
   - Replace absolute `file:///` links with relative links in Story and Tech-Spec for portability.
   - Add “event loop / echo” guardrails: reconcile must not trigger Yjs edge writes (explicitly require GraphSyncManager to ignore hierarchical edges and/or tag reconcile edges as local-only).
   - Call out existing tests to update (GraphSyncManager hierarchical-edge tests) and list them in the test matrix.
3. Consider:
   - Add a lightweight safety net despite “no migration”: if hierarchical edges exist in Yjs, ignore (or optionally delete) them on load to prevent drift.
   - Consider a feature flag to disable canvas reparent-on-mouseup in non-free modes if regression risk remains high (per RCA mitigation suggestions).
